{{>layout/header}}

    <!-- 목록 출력 영역 -->
        <table class="table table-horizontal table-bordered">
            <thead class="thead-strong">
            <tr>
                <th>번호</th>
                <th>제품명</th>
                <th>가격</th>
            </tr>
            </thead>
            <tbody id="tbody">
            {{#list}}
                <tr>
                    <td>{{prodId}}</td>
                    <td><a href="/posts/update/{{prodId}}">{{prodName}}</a></td>
                    <td>{{prodPrice}}</td>
                </tr>
            {{/list}}
            </tbody>
        </table>
        <a href="/" role="button" class="btn btn-secondary">처음으로</a>
{{>layout/footer}}